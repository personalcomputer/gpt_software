#include "Audio.h"

void Audio::loadSounds() {
    // Load sound buffers into memory
}

void Audio::playSound(const std::string& soundName) {
    // Play a specific sound by name
}

void Audio::playMusic(const std::string& musicName) {
    // Play a specific music track by name
}

void Audio::stopMusic() {
    // Stop playing the current music track
}
