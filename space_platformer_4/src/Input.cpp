#include "Input.h"

void Input::update(sf::RenderWindow& window) {
    // Update pressedKeys vector based on current key state
}

bool Input::isKeyPressed(sf::Keyboard::Key key) {
    // Check if a specific key is currently pressed
}
